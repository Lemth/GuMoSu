matrix = matrix_build(input[0],input[1],input[2],input[3],input[4],input[5],input[6],input[7],input[8]);
for (var i = 0; i < 16; i++)
{
    with (list_matrix_cells[|i])
    {
        value = other.matrix[my_index];
        text = string(value);
    }
}

